# Professor packet

This is a gated research handoff for the Pineland insurgency agent-based simulation program. It contains implementation evidence, synthetic inference validation, a fixed-work performance record, theory specification, and a failure ledger.

Historical transfer claims are withheld unless the phase-C contract authorizes the once-only Nepal/Afghanistan confrontation under the live model hash. Prior stale-core logs are preserved as provenance and are not promoted.

## Current source of truth

Read `10_current_science_status.md` and `11_current_claims_and_evidence.md` first. They supersede the static PDF summaries for the current run. The existing PDFs were generated before the optimized canonical v5 benchmark and were intentionally not regenerated after the user's no-future-PDF instruction.

The current complete fixed workload is `studies/research_program/phase_a_fixed_work_benchmark_v5.json`: 768 PWB, warmup plus five measured repetitions, median 5.583900 PWB/s, CV 0.032946, E1 passed, and E2 remains unmet. The result is a complete measured gate, not an extrapolation.

## Review order

1. Read `10_current_science_status.md` and `11_current_claims_and_evidence.md`.
2. Inspect `12_current_reproducibility_manifest.json` and verify hashes. `07_reproducibility_manifest.json` is retained as the earlier packet manifest.
3. Read the legacy PDF summaries only for architecture/background context.
4. Check the fixed workload and every gate in the current status artifact.
5. Read `09_independent_review.json` for the final integrity checklist.

The complete fixed workload is recorded in `studies/research_program/phase_a_fixed_work_benchmark_v5.json`; its explicit E2-negative record is `studies/research_program/phase_a_fixed_work_benchmark_failure_v4.json`. E1 is met; E2 remains a reported target, not a gate.

No external outreach was performed.
