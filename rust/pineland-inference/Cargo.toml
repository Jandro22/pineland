[package]
name = "pineland-inference"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"

[features]
default = ["rayon"]
rayon = ["dep:rayon"]

[dependencies]
pineland-core = { path = "../pineland-core" }
pineland-model = { path = "../pineland-model" }
rayon = { version = "1.11", optional = true }

[lib]
path = "src/lib.rs"
