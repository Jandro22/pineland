# Legacy configuration fixtures

These configs were formerly loose at repository root and are retained because
they capture earlier native/parity and scaling test conditions. They are not the
canonical user-facing scenarios; new hand-authored scenarios belong in
`scenarios/`, while configs used solely by automated tests belong under
`tests/fixtures/`.
